import { ResizableSidenavDirective } from './resizable-sidenav.directive';

describe('ResizableSidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableSidenavDirective();
    expect(directive).toBeTruthy();
  });
});
